"""nidra.eval — published, reproducible benchmark harnesses.

The house rule, learned from a market where vendor numbers failed reproduction:
**the harness ships before the number.** Anything reported here can be re-run
by a stranger with one command; anything requiring an API key is labeled, and
keyless metrics are preferred wherever they are honest.
"""
